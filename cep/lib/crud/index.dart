import 'package:cep/crud/home_page.dart';
import 'package:flutter/material.dart';

class AppCrudPessoa extends StatelessWidget {
  const AppCrudPessoa({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: HomePage(),
    );
  }
}
